services:
  portfolio:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: daniel-portfolio-app
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - portfolio-network
networks:
  portfolio-network:
    driver: bridge